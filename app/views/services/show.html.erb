<div class="container">
  <p>
    <strong>Name:</strong>
    <%= @service.name %>
  </p>
  <p>
    <strong>Description:</strong>
    <%= @service.description %>
  </p>
  <% if user_signed_in? %>
    <%= link_to 'Edit', edit_service_path(@service) %> |
    <%= link_to 'Back', services_path %>
  <% else %>
  <% end %>
</div>
